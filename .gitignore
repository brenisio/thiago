# Planilhas com dados de clientes (PII) — NÃO versionar
*.xlsx
*.xls
*.csv

# Banco local com dados de clientes (PII) — NÃO versionar
banco.json

# Artefatos de teste / temporários
.playwright-mcp/
*.png
